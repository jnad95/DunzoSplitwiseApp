import datatype.Person;

import java.util.List;

public class LoanSimplifierImpl implements LoanSimplifier{
    @Override
    public List<Person> simplify(List<Person> participants) {
        return null;
    }
}
